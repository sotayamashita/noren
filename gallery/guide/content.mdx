import { Features, Feature } from "@/components/guide/features";

# Heading 1

Body text with a [link](https://example.com), **bold**, _italic_, `inline code`, and a keyboard shortcut like <kbd>⌘</kbd> <kbd>K</kbd>. Paragraphs wrap at the Typeset measure and keep the rhythm set by `--typeset-flow`.

## Heading 2

### Heading 3

#### Heading 4

- Unordered list item
- Another item with `code`
  - Nested item
  - Nested item

1. Ordered step one
2. Ordered step two
3. Ordered step three

> A blockquote. Typeset draws the rule on the leading edge and keeps the text colour.

| Flag        | Effect                          | Default |
| ----------- | ------------------------------- | ------- |
| `--ci`      | Headless, auto-approve the plan | off     |
| `--headed`  | Watch the browser while it runs | off     |
| `--timeout` | Give up after N minutes         | 30      |

```bash
npx -y noren@latest init
```

```json
{
  "mcpServers": {
    "noren": { "command": "npx", "args": ["-y", "noren@latest", "mcp"] }
  }
}
```

```tsx
export function Hello({ name }: { name: string }) {
  return <p>Hello, {name}</p>;
}
```

<Features>
  <Feature title="Wide card with a visual" visual="window" wide>
    Spans two columns; the band above clips the still life.
  </Feature>
  <Feature title="Terminal" visual="terminal">
    One column.
  </Feature>
  <Feature title="List" visual="list">
    One column.
  </Feature>
  <Feature title="Feedback" visual="feedback">
    One column.
  </Feature>
  <Feature title="Languages" visual="languages">
    One column.
  </Feature>
  <Feature title="Theme" visual="theme">
    One column.
  </Feature>
  <Feature title="Files" visual="files">
    One column.
  </Feature>
  <Feature title="No visual">
    Text only, for products without a matching still life.
  </Feature>
</Features>

---

Text after a horizontal rule, followed by an image.

![The generated Open Graph image](/opengraph-image)
